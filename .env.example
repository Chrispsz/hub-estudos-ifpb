# Copie para .env e preencha UMA das opções (o tutor funciona com qualquer uma)

# Opção A — OpenRouter (recomendado: dezenas de modelos grátis com 1 key)
# https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Opção B — API pública da Z.ai (tier grátis no glm-4.5-flash)
# https://z.ai
ZAI_API_KEY=
ZAI_BASE_URL=https://api.z.ai/api/paas/v4
ZAI_MODEL=glm-4.5-flash

# Prisma (scaffold opcional — nada no runtime usa banco hoje)
DATABASE_URL="file:./db/custom.db"
